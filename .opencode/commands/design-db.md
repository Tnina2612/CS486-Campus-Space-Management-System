---
description: Run the complete database design pipeline based on the business requirement file.
---

Use the database design pipeline skill in:

`.opencode/skills/db-design-pipeline/SKILL.md`

Read the business requirement from:

`$ARGUMENTS`

Run the full pipeline and generate/update all required outputs in the `outputs/` folder.