---
description: Run the Phase 2 database extension pipeline (schema migration, concurrency control, and index tuning) based on the updated requirements.
---

Use the Phase 2 execution pipeline skill in:

`.opencode/skills/phase2-pipeline/SKILL.md`

Read the updated business requirement from:

`$ARGUMENTS`

Run the full pipeline to generate all required Phase 2 outputs in the `outputs/` folder, strictly preserving the Phase 1 baseline files.